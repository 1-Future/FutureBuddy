/**
 * VoiceAccessGrid is now handled entirely by the native accessibility service overlay.
 * This component is kept as a no-op to avoid breaking imports.
 */
export default function VoiceAccessGrid() {
  return null;
}
